#!/usr/bin/env python

import requests
import sys

headers = {
    'Accept': 'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.9',
    'Accept-Language': 'en-GB;q=0.6',
    'Cache-Control': 'no-cache',
    'Connection': 'keep-alive',
    'Origin': 'https://wifi.gsb.gov.tr',
    'Pragma': 'no-cache',
    'Referer': 'https://wifi.gsb.gov.tr/login.html',
    'Sec-Fetch-Dest': 'document',
    'Sec-Fetch-Mode': 'navigate',
    'Sec-Fetch-Site': 'same-origin',
    'Sec-Fetch-User': '?1',
    'Sec-GPC': '1',
    'Upgrade-Insecure-Requests': '1',
    'User-Agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 11_12) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/99.0.4880.113 Safari/537.36',
}

data = {
    'j_username': '***REMOVED***',
    'j_password': '***REMOVED***',
    'submit': 'Login',
}

response = requests.post('https://wifi.gsb.gov.tr/j_spring_security_check', headers=headers, data=data)

if response.status_code == 200:
    print("LOGGED IN")
    os.exit(0)
else:
    print("ERROR")
    sys.exit(1)
